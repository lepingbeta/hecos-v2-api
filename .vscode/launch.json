{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            // "program": "${fileDirname}/../inner-user-center-api/main.go",
            "program": "${workspaceFolder}",
            // "env": {
            //     "MY_ENV_VAR": "someValue" // 设置环境变量
            // },
            // "args": ["arg1", "arg2"] // 传递给程序的命令行参数
        }
    ]
}